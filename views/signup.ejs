<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <title>Sign Up - Join Us</title>
    <style>
        body {
            background-color: #0d1117;
            color: #c9d1d9;
            font-family: 'Poppins', sans-serif;
        }

        .container {
            max-width: 420px;
            margin: 80px auto;
            padding: 25px;
            background-color: #161b22;
            border-radius: 10px;
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.6);
            text-align: center;
        }

        h2 {
            color: #f5a623;
            margin-bottom: 15px;
        }

        .form-group {
            text-align: left;
            margin-bottom: 15px;
        }

        .form-group label {
            font-weight: 500;
            color: #f5a623;
            margin-bottom: 5px;
            display: block;
        }

        .form-control {
            background-color: #21262d;
            color: #c9d1d9;
            border: 1px solid #30363d;
            border-radius: 6px;
            padding: 10px;
            transition: border-color 0.3s ease-in-out;
        }

        .form-control:focus {
            border-color: #f5a623;
            background-color: #21262d;
            color: #c9d1d9;
        }

        .form-text {
            color: #8b949e;
            font-size: 0.9rem;
        }

        .btn-primary {
            background-color: #f5a623;
            color: #000;
            font-weight: bold;
            padding: 10px;
            width: 100%;
            border-radius: 6px;
            border: none;
            transition: background-color 0.3s ease-in-out;
        }

        .btn-primary:hover {
            background-color: #e59400;
        }

        .signup-message {
            font-size: 0.95rem;
            margin-bottom: 20px;
            color: #a1a1a1;
        }

        .login-link {
            margin-top: 15px;
            font-size: 0.9rem;
        }

        .login-link a {
            color: #f5a623;
            text-decoration: none;
        }

        .login-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <%- include('partials/nav') %>

    <div class="container">
        <h2>Create an Account</h2>
        <p class="signup-message">Join us today and explore the best content. It's quick and easy!</p>

        <form action="/user/signup" method="POST">
            <div class="form-group">
                <label for="fullName">Full Name</label>
                <input type="text" class="form-control" id="fullName" name="fullName" placeholder="Enter your full name" required>
            </div>

            <div class="form-group">
                <label for="exampleInputEmail1">Email Address</label>
                <input type="email" class="form-control" id="exampleInputEmail1" name="email" aria-describedby="emailHelp" placeholder="Enter your email" required>
                <small id="emailHelp" class="form-text">We’ll never share your email with anyone else.</small>
            </div>

            <div class="form-group">
                <label for="exampleInputPassword1">Password</label>
                <input type="password" class="form-control" id="exampleInputPassword1" name="password" placeholder="Enter a strong password" required>
            </div>

            <button type="submit" class="btn btn-primary">Sign Up</button>
        </form>

        <p class="login-link">Already have an account? <a href="/user/login">Login here</a></p>
    </div>

    <%- include('./partials/scripts.ejs') %>  
</body>
</html>
