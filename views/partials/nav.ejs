<nav class="navbar navbar-expand-lg custom-navbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">ThoughtHub</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link home-btn" aria-current="page" href="/">Home</a>
                </li>
                
                <% if (locals.user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/blog/add-new">Add Blog</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle user-dropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                          <%= user ? user.email : 'Guest' %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item logout-link" href="/user/logout">Logout</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link signup-btn" href="/user/signup">SIGN UP</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link signin-btn" href="/user/signin">SIGN IN</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
  </nav>
  
  <% if (locals.error) { %>
    <div class="alert alert-danger text-center fade-in-alert" role="alert">
        <%= locals.error %>
    </div>
  <% } %>
  
  <style>
    /* Custom Navbar Styling */
    .custom-navbar {
        background: rgba(30, 30, 30, 0.8);
        backdrop-filter: blur(12px);
        border-bottom: 2px solid rgba(245, 166, 35, 0.6);
        padding: 14px 20px;
        transition: all 0.3s ease-in-out;
    }
  
    .navbar-brand {
        font-weight: 700;
        color: #f5a623 !important;
        font-size: 1.6rem;
        letter-spacing: 1px;
        transition: color 0.3s ease-in-out;
    }
  
    .navbar-brand:hover {
        color: #ffb84d !important;
    }
  
    .nav-link {
        color: #e0e0e0;
        font-weight: 500;
        transition: all 0.3s ease-in-out;
        padding: 10px 15px;
        border-radius: 6px;
    }
  
    .nav-link:hover {
        color: #f5a623;
        background: rgba(255, 165, 0, 0.15);
    }
  
    /* User Dropdown */
    .user-dropdown {
        color: #f5a623;
        font-weight: bold;
    }
  
    .dropdown-menu {
        background-color: #1e1e1e;
        border-radius: 8px;
        padding: 10px;
        box-shadow: 0px 5px 20px rgba(0, 0, 0, 0.3);
        transition: all 0.3s ease-in-out;
    }
  
    .dropdown-item {
        color: #e0e0e0;
        padding: 10px 15px;
        transition: all 0.3s ease-in-out;
        border-radius: 6px;
    }
  
    .dropdown-item:hover {
        background-color: #f5a623;
        color: #121212;
    }
  
    /* Signup & Signin Buttons */
    .signup-btn, .signin-btn {
        font-weight: bold;
        border-radius: 6px;
    }
  
    .signup-btn:hover, .signin-btn:hover {
        background-color: rgba(255, 165, 0, 0.2);
    }
  
    /* Logout Button */
    .logout-link {
        color: #ff4d4d;
        font-weight: bold;
    }
  
    .logout-link:hover {
        background-color: #ff4d4d;
        color: white;
    }
  
    /* Error Alert Styling */
    .fade-in-alert {
        margin: 20px auto;
        max-width: 500px;
        border-radius: 8px;
        font-weight: bold;
        opacity: 0;
        animation: fadeIn 0.6s forwards;
    }
  
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
    }
  </style>
  